#include<stdio.h>

int hai(int n)
{
    int count=0;
    for(int i=2;i<=n;i++)
    {
        if(n%i==0)
        {
            count=1;
        }
    
    if(count==0)
    { 
            printf("%d \n",n);
    }
        hai(n-1);
        return count;
    }
    
}
 void main()
{
    int a;
    int count=0;
    printf("enter the number");
    scanf("%d",&a);
    hai(a);
    printf("the prime numberup to n is %d",a);
}
