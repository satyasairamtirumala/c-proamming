#include<stdio.h>
#include<string.h>
void main()
{
     char c[20],pwd;
     int i=0;
    printf("enter the password");

    while((c[i]=getch(c))!=13)
    {

        printf("*");
        i++;
    }
    printf("the entered password is %s",c);

}